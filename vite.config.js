import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/RestiFauziMukhliza-React/', // Sesuaikan dengan URL GitHub Pages Anda
});
